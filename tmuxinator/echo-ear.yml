name: all
root: ~/lymph-talk
pre_window: . venv/bin/activate && source env && clear

windows:
    - lymph:
        layout: main-vertical
        panes:
            - client:
                - toilet client
            - echo:
                - toilet echo1
                - lymph instance --config=conf/echo.yml --reload
            - echo:
                - toilet echo2
                - lymph instance --config=conf/echo.yml --reload
            - ear:
                - toilet ear
                - lymph instance --config=conf/ear.yml --reload
