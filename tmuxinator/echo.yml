name: echo
root: ~/lymph-talk
pre_window: . venv/bin/activate && source env && clear

windows:
    - lymph:
        layout: main-vertical
        panes:
            - client:
                - toilet client
            - echo:
                - toilet echo
                - lymph instance --config=conf/echo.yml --reload
